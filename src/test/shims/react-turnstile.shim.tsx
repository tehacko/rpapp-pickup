import { forwardRef } from 'react';

export const Turnstile = forwardRef(function TurnstileShim(
  _props: Record<string, unknown>,
  _ref: unknown,
): null {
  return null;
});

Turnstile.displayName = 'TurnstileShim';
